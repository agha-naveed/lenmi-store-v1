import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['www.hamzastore.pk', 'www.space-tech.co', 'm.media-amazon.com', 'typeshop.pk', 'img.drz.lazcdn.com', "mrlaptop.pk"]
  }
};

export default nextConfig;
